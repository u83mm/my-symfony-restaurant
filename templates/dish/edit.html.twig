{% extends 'base.html.twig' %}

{% block title %}{{ 'edit_dish'|trans|title }}{% endblock %}

{% block main %}
    <h1>{{ 'edit_dish'|trans|title }}</h1>
    <div class="col-12 col-md-9 col-lg-7 col-xl-6 mx-auto">
        
        {{ include('dish/_form.html.twig', {'button_label': 'update'|trans|capitalize}) }}
    
        <a class="btn btn-primary" href="{{ path('app_dish_index') }}">{{ 'go_back'|trans|capitalize }}</a>
    
        {{ include('dish/_delete_form.html.twig') }}
    </div>    
{% endblock %}
