<form method="post" action="{{ path('app_dish_day_delete', {'id': dish_day.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dish_day.id) }}">
    <button class="btn">Delete</button>
</form>
