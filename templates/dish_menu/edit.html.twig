{% extends 'base.html.twig' %}

{% block title %}Admin | {{ 'dish categories'|trans|capitalize }}{% endblock %}

{% block main %}
    <h1>{{ 'edit category'|trans|title }}</h1>

    <div class="col-12 col-md-9 col-lg-7 col-xl-6 mx-auto mb-3">

        {{ include('dish_menu/_form.html.twig', {'button_label': 'update'|trans|capitalize}) }}
        
    </div>
    <div class="col-12 col-md-9 col-lg-7 col-xl-6 mx-auto">
        <div class="col-2 d-inline-block float-start">
            <a class="btn btn-primary" href="{{ path('app_dish_menu_index') }}">{{ 'go_back'|trans|capitalize }}</a>
        </div>
        <div class="col-2 d-inline-block float-start">
            {{ include('dish_menu/_delete_form.html.twig') }}
        </div>  
    </div>             
    
{% endblock %}
