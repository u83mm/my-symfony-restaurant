{% extends 'base.html.twig' %}

{% block title %}DishMenu{% endblock %}

{% block main %}
    <h1>DishMenu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dish_menu.id }}</td>
            </tr>
            <tr>
                <th>MenuCategory</th>
                <td>{{ dish_menu.menuCategory }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dish_menu_index') }}">back to list</a>

    <a href="{{ path('app_dish_menu_edit', {'id': dish_menu.id}) }}">edit</a>

    {{ include('dish_menu/_delete_form.html.twig') }}
{% endblock %}
