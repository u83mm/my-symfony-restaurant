# Ejecutar:
	- docker-compose build
	- docker-compose up -d

# Esta plantilla está preparada con una versión de Symfony 6.1, en caso de salir una nueva versión y la queramos sustituir simplemente
  haremos lo del punto siguiente
  
# Entrar en el contenedor
	- docker exec -it "nombre del contenedor php" bash
	- composer create-project symfony/skeleton:"Versión symfony (ej. 6.1.*)" myApp (dónde myApp) es el projecto creado.
	
# Mover todos los archivos de myApp a /var/www/
	- sudo mv * /ruta/al/proyecto
	
# Cambiar propietario del directorio /Proyecto en máquina anfitrión (no en el contenedor)
	- chown mario:mario -R /Proyecto
	
# Cambiar propietario del directorio /public
	- chown www-data:www-data -R /var/www/public
